const Tabs = ({options})  => {
    return (
        <ul>
            {options.map((op)=>{
                return (
                    <li>
                        
                    </li>
                )
            })}
        </ul>

    )
}

export default Tabs;